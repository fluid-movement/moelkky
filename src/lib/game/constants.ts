/** Reaching exactly this score wins the game. */
export const WIN_SCORE = 50;

/** Overshooting {@link WIN_SCORE} resets a player's score to this value. */
export const OVERSHOOT_RESET = 25;

/** Consecutive misses (throws scoring 0) that eliminate a player when the rule is on. */
export const MAX_MISSES = 3;

/** Minimum / maximum points scorable in a single Mölkky throw. */
export const MIN_THROW = 0;
export const MAX_THROW = 12;
